import Login from '@/components/login';// Import useRouter to handle redirects
import React from 'react';

const LoginPage = () => {
  

  return (
    <div>
     
      <Login />
    </div>
  );
};

export default LoginPage;
